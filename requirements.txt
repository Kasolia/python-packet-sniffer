scapy

## Installation

Clone the repository:

```bash
git clone https://github.com/Kasolia/python-packet-sniffer.git
cd python-packet-sniffer
```

Install dependencies:

```bash
pip install -r requirements.txt
```